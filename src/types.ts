// Type definitions for the IDL interfaces
export interface FeeManager {
  version: string;
  name: string;
  instructions: any[];
  accounts: any[];
  events: any[];
  errors: any[];
}

export interface Governor {
  version: string;
  name: string;
  instructions: any[];
  accounts: any[];
  events: any[];
  errors: any[];
}

export interface RewardPool {
  version: string;
  name: string;
  instructions: any[];
  accounts: any[];
  events: any[];
  errors: any[];
}

export interface RewardPoolDynamic {
  version: string;
  name: string;
  instructions: any[];
  accounts: any[];
  events: any[];
  errors: any[];
}

export interface StakePool {
  version: string;
  name: string;
  instructions: any[];
  accounts: any[];
  events: any[];
  errors: any[];
} 